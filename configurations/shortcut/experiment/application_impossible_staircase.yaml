name: application_impossible_staircase

experiment:
  tasks: [validation]
  validation:
    batch_size: 1

dataset:
  frame_skip: 1
  n_frames: 120
  num_eval_videos: 10

algorithm:
  loop_closure:
    enabled: true
  tasks:
    prediction:
      manual_window_indices: [[118,119,0,1,2,3,4,5],[114,115,116,117,118,119,0,1]]
      repeat_manual_window_indices: 2
      penrose_staircase: true