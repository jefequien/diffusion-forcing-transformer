name: limitation_ours_forward_orbit_backward_loopclose

experiment:
  tasks: [validation]
  validation:
    batch_size: 1

dataset:
  frame_skip: 1
  n_frames: 24
  num_eval_videos: 10

algorithm:
  loop_closure:
    enabled: true
  tasks:
    prediction:
      manual_window_indices: [[18,19,4,5,6,7,16,17],[22,23,0,1,2,3,20,21]]
      repeat_manual_window_indices: 1