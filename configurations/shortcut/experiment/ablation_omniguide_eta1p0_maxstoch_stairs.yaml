name: ablation_omniguide_eta1p0_maxstoch_stairs

experiment:
  tasks: [validation]
  validation:
    batch_size: 1

# override dataset parameters
dataset:
  frame_skip: 1
  n_frames: 24
  num_eval_videos: 40