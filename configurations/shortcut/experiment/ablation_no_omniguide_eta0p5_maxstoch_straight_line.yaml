name: ablation_no_omniguide_eta0p5_maxstoch_straight_line

experiment:
  tasks: [validation]
  validation:
    batch_size: 4

# override dataset parameters
dataset:
  frame_skip: 5
  n_frames: 48
  num_eval_videos: 40
