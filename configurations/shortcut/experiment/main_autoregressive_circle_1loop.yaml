name: main_autoregressive_circle_1loop

experiment:
  tasks: [validation]
  validation:
    batch_size: 1

dataset:
  frame_skip: 1
  n_frames: 24
  num_eval_videos: 40

algorithm:
  loop_closure:
    enabled: true
