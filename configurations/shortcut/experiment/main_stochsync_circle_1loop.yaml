name: main_stochsync_circle_1loop

experiment:
  tasks: [validation]
  validation:
    batch_size: 4

dataset:
  frame_skip: 1
  n_frames: 24
  num_eval_videos: 40

algorithm:
  loop_closure:
    enabled: true